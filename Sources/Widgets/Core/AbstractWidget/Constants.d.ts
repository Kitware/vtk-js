/**
 * Priority at which widgets register their interactor observers, so that they
 * are handled before the interactor style.
 */
export declare const WIDGET_PRIORITY: number;

declare const _default: {
  WIDGET_PRIORITY: typeof WIDGET_PRIORITY;
};
export default _default;
